git branch second                   // create 'second' branch
git branch                          //check branch 
git checkout second                 //branch switch
git switch second                   //branch switch
git checkout -b third               //Check if branch doesnot exists then Create branch and switch in it
git switch -c four                  //Check if branch doesnot exists then Create branch and switch in it (switch is modern commnd)
git branch -D four                  //Delete 'four' branch
git push -u origin four             //Create branch on github and push code into four branch at github
